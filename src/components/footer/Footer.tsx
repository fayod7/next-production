import { memo } from 'react';

const Footer = () => {
  return (
    <footer className="w-full">
    <nav className="container">
      <h2>Footer</h2>
    </nav>
    </footer>
  );
};

export default memo(Footer);